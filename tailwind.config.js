module.exports = {
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
      fontSize: {
        xxs: ".5rem",
      },
    },
  },
  plugins: [require("@tailwindcss/typography")],
}
